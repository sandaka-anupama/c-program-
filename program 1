#include<stdio.h>
int main()
{
	int arr[30];
	int n,i,flag=0;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
	}
	int num;
	while(n>1)
	{
		if(i==num)
		{
			flag=1;
			
		}n--;
	}
	if(flag==0)
	printf("UNIQUE\n");
	else
	printf("DUPLICATE\n");
	}
